<!-- waypoint:start -->
## Execution Harness

Implementation work tracked in the root `issues/` directory must follow `issues/WORKFLOW.md`. Before starting or resuming tracked work, read `issues/AGENTS.md`, `issues/README.md`, and the active issue file. The issue file is the durable handoff state; conversation history is not authoritative.
<!-- waypoint:end -->
